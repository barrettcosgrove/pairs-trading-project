# Template for API keys or environment-specific settings. Copy to .env locally. The .env file is gitignored.
# Alpha Vantage — free at alphavantage.co
ALPHA_VANTAGE_KEY=your_key_here

# Set to 1 if on campus WiFi
ARQ_YF_DISABLE_PROXY=1